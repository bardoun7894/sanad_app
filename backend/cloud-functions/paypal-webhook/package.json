{
  "name": "paypal-webhook",
  "version": "1.0.0",
  "description": "PayPal subscription webhook handler for Sanad App",
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.1"
  },
  "devDependencies": {},
  "scripts": {
    "deploy": "gcloud functions deploy paypalWebhook --gen2 --runtime nodejs18 --trigger-http --allow-unauthenticated",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "paypal",
    "webhook",
    "firebase",
    "subscription"
  ],
  "author": "Sanad Team",
  "license": "MIT"
}
