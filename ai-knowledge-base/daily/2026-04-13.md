# Daily Log: 2026-04-13

## Sessions

## Memory Maintenance

### Session (09:43)

**Context:** Implementing and fixing the Sanad App payment system (Google Pay, Apple Pay, Visa card, Bank Transfer screens).

**Key Exchanges:**
- Spec Kit was bootstrapped (`.specify/templates/` + `specs/` directory) as part of session startup.
- NotebookLM MCP returned 400 errors (stale session ID); docs were read directly from the filesystem instead.
- `flutter analyze` confirmed 0 errors / 0 warnings across all changed files after each round of edits.

**Decisions Made:**
- Google Pay gateway changed from `"example"` to `"paypal"` and environment set to `PRODUCTION`.
- Apple Pay screen created from scratch (`apple_pay_screen.dart`) since only a `// TODO: redirect to card` stub existed; routed via `app_router.dart`.
- Bank transfer reference code made dynamic: `REF-{UID6}-{YYYYMMDD}-{SUFFIX}` per user per session (was hardcoded).
- Bank detail strings moved from hardcoded UI to locale files (`app_strings.dart`, `app_strings_en.dart`, `app_strings_fr.dart`) with `YOUR_*` placeholders, so real values can be filled without touching Dart code.
- PayPal WebView cancel detection narrowed from `url.contains('cancel')` to `url.startsWith('sanad://payment/cancel')` to prevent false-positive cancellations.

**Lessons Learned:**
- The `_buildCardFormHtml` method and `html` variable in `card_payment_screen.dart` were pre-existing dead code — clean it when touching the file.
- Apple Pay requires a domain verification file hosted on the server in addition to registering the Merchant ID in Apple Developer portal.
- NotebookLM MCP 400 errors are caused by stale session cache; fallback is to read source docs directly from disk.

**Action Items:**
- Fill in `YOUR_PAYPAL_MERCHANT_ACCOUNT_ID` in `assets/payment_profiles/default_payment_profile_google_pay.json` (found in PayPal dashboard → Account Settings → Business Profile).
- Fill in real Apple Pay Merchant Identifier (`merchant.app.sanad` placeholder) in `assets/payment_profiles/default_payment_profile_apple_pay.json` and host domain verification file.
- Replace `bankAccountName`, `bankAccountNumber`, `bankSwiftCode`, `bankIban` placeholders in all 3 locale files with real bank details.
- Run `nlm login` to refresh NotebookLM MCP session when 400 errors occur.

### Memory Flush (23:15)

FLUSH_OK - Nothing worth saving from this session

