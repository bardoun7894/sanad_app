Create a structured git commit with documentation

Helps maintain a clean commit history and automatically:
1. Creates/updates a feature doc in `docs/` folder
2. Stages all changes
3. Creates a git commit with feature branch workflow
4. Documents what was changed and why

Usage: `/git-commit` to interactively create a commit with docs
